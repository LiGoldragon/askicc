;; Instance.synth — after _@_ in statement
;;
;; @ declares a type instance. PascalCase name = the type.
;; Optional (Type) annotation before the expression.
;; Bare @Name in expression position is a reference (ExprAtom).

// @Name (<Type>) <Expr>
// @Name <Expr>
