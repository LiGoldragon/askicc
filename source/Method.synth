;; Method.synth — inside (method ...)
;;
;; Parameters, optional return type, then body.
;; Body can be any process delimiter directly.

+<Param>
?<Type>
// [<Body>]
// (|<Match>|)
// [|<Loop>|]
// {|<Expr> [<Body>]|}
// {<StructConstruct>}
