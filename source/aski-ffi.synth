;; aski-ffi.synth — inside {|PascalName/ funcs|}
;;
;; {|Cast/
;;   (toF32/ @Self F32)
;;   (toU32/ @Self U32)
;; |}

(CtxKind/ FfiBlock FfiFunc)

/

(LParen FfiBlock camelName/ FfiFunc Space)
;; (toF32/ @Self F32) — FFI function declaration

;; Rust extern name derived via toSnake from the aski name.
