;; aski-struct.synth — inside {PascalName/ fields}
;;
;; {Point/ Horizontal F64 Vertical F64}

(CtxKind/ Struct)

/

(LBrace Struct PascalName/ TypeParams Space)
;; Vec{Element} — parameterized type in field type ref

(TraitBoundOpen Struct/ TraitBound None)
;; {|display|} — trait bound in field type ref

;; Fields are pairs: PascalName TypeRef (no delimiter transitions needed)
