;; IterationSource.synth — source and binding for {| |}
;;
;; {| self.List.item [StdOut:print(item.Name)] |} — iterate over
;; the List field of self, binding each element as `item`.

<Expr>.@Binding
