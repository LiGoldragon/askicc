;; Method.synth — method definition (v0.19).
;;
;; Signature + optional body. When body is absent, this is a
;; signature-only declaration (required trait method). When body
;; is present, this is a default trait-method or full impl.

<Signature>
?<MethodBody>
