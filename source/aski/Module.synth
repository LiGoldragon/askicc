;; Module.synth (v0.20) — inside (Module ...).
;;
;; Module exports retired — visibility is now declaration-local
;; via the `@` sigil. Module header carries only the name and
;; optional imports.

*#Import#[ :Module *:ImportedName ]
